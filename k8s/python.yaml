apiVersion: batch/v1
kind: CronJob
metadata:
  name: python-etl-job
spec:
  schedule: "0 * * * *" 
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: python-etl
            image: tarsuslu/python-project:latest
          restartPolicy: OnFailure